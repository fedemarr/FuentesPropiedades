import { FormularioPropiedad } from "@/components/admin/propiedades/formulario-propiedad";

export const metadata = {
  title: "Nueva propiedad",
};

export default function NuevaPropiedadPage() {
  return <FormularioPropiedad id={null} />;
}
